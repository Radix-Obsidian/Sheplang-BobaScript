{
  "name": "sheplang",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "shep": "node ./packages/cli/dist/index.js"
  }
}
